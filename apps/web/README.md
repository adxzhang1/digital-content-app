# Web App

Next.js frontend for the digital content app.

## Local Setup

Copy `.env.example` to `.env.local` and fill in the deployed API URL and Firebase web app config.

```sh
pnpm --filter web dev
```

The app runs on `http://localhost:3000`.
